# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr

# Environment files — never commit secrets
.env
.env.*
!.env.example

# Platform-specific native bindings — these must NEVER be committed
# They are resolved automatically by npm based on the target OS
@rolldown/binding-win32-*
@rolldown/binding-darwin-*
@rolldown/binding-linux-*

# Nixpacks cache
.nixpacks

# Local dev artifacts
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# macOS
.AppleDouble
.LSOverride
Thumbs.db
