# nixpacks.toml — Coolify/Nixpacks deployment configuration for AgencyOS
# Vite 8 requires Node.js >= 20.19.0. We pin Node 22 LTS for stability.

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
